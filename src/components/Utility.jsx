import React from 'react'

function Utility({children}) {
  //Reusable for other pages if needed
  return (
    //Reusable for other pages if needed
    <main className="Utility">{children}</main>
  )
}

export default Utility;